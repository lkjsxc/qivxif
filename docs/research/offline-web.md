# Offline Web

## Facts

- Service workers intercept requests and can serve cached app shell assets.
- Cache API stores app shell and safe HTTP responses.
- IndexedDB stores structured client data asynchronously with transactions.

## Implication

qivxif stores graph, ops, text snapshots, cursors, and cache metadata in IndexedDB.
