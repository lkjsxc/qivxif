# Open Questions

## Client

- Which editor widget best preserves qivxif text events after the hardened editor shell is stable?

## Protocol

- Which binary framing should replace JSON framing after the protocol is proven?
- Which live transport backup should be documented if WebTransport is unavailable and HTTP polling is too slow?

## Product Defaults

- What default local media cache budget should ship?
- Should profile pages be public by default, unlisted by default, or private until explicitly published?

These questions do not block the server, graph, sync, auth, media, and offline client slices.
