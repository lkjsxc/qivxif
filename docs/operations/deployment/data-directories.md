# Data Directories

## Server

- Default data directory: `data/`.
- Default redb file: `data/qivxif.redb`.

## Tests

- Tests use `tmp/` or container-local temporary directories.
- Test data is not durable canon.
