# Repair

## CLI

`qivxifctl store check` validates graph, history, blob, feed, and cursor references.

## Rule

Repair reports are structured and must not mutate data unless an explicit repair command is added.
